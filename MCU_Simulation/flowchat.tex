\documentclass{article}
\usepackage[margin=2.5cm]{geometry}
\usepackage{tikz}
\usepackage{kotex}
\usepackage{amsmath}
\usetikzlibrary{calc, arrows.meta, positioning}

\tikzstyle{block} = [rectangle, draw=black, minimum width=3cm, minimum height=2cm, text centered]
\tikzstyle{arrow} = [thick,->,>=stealth]

\begin{document}

\begin{center}
\begin{tikzpicture}[node distance=1.0cm and 4.5cm]

% 맨 윗줄
\node (user1) [block] {user};
\node (server1) [block, right=of user1] {server};
\draw [arrow] (user1) -- (server1);
\node at ($(user1)!0.5!(server1)+(0,0.25)$) [align=center] {$H(PW)$};

% 두 번째 줄
\node (server2) [block, below=of server1] {server};
\draw [arrow] (server1) -- (server2);

\node (db) [block, right=of server2] {DB};
\draw [arrow] (server2) -- (db);
\node at ($(server2)!0.5!(db)+(0,0.25)$) [align=center] {
    $H(\text{mcu\_uid}),H(\text{auth})$

};

% 세 번째 줄
\node (server3) [block, below=of server2] {server};
\draw [arrow] (server2) -- (server3);

\node (user2) [block, left=of server3] {user};
\draw [arrow] (server3) -- (user2);

\node at ($(user2)!0.5!(server3)+(0,-0.5)$) [align=center] {
    $C_0 = E_{k_{PW}}(H(\text{auth}))$ \\
    $k_{PW} = H(PW)$
};

\end{tikzpicture}
\end{center}

\end{document}
